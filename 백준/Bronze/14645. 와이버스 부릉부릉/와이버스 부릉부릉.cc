#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, K, A, B;
    cin >> N >> K;
    for(int i = 0; i < N; ++i){
        cin >> A >> B;
    }
    cout << "비와이";
    return 0;
}